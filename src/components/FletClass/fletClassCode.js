export const class1FCode = [
    `pip install virtualenv`, 
    `tu direccion de carpeta c: -m pip install --upgrade pip`, 
    `python -m venv prueba`,
    `C:\Users\(nombre de usuario)\AppData\Local\Programs\Python\Python(n° de version)\python.exe python -m venv (nombre del entorno virtual)`,
    `import flet as ft`,
    `def main(page: ft.Page):  # Define la función principal`,
    `page.title = "Mi App en Flet"  # Cambia el título de la ventana`,
    `page.add(ft.Text("¡Hola, mundo en Flet!"))  # Agrega un texto a la página`,
    `ft.app(target=main)  # Inicia la aplicación llamando a 'main'`,
    `python main.py`,
    `flet run --web main.py`,
]

export const class2FCode = [
    `contenedor = ft.Container(
        content=ft.Text("¡Hola, soy un Container!",color=ft.colors.BLACK),
        bgcolor=ft.Colors.AMBER,
        padding=20,
        border_radius=10
    )
    page.add(contenedor)`
]